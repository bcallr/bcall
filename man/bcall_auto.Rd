% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcall_main.R
\name{bcall_auto}
\alias{bcall_auto}
\title{B-Call Analysis with Automatic Clustering}
\usage{
bcall_auto(
  rollcall,
  distance_method = 1,
  pivot = NULL,
  threshold = 0.1,
  verbose = TRUE
)
}
\arguments{
\item{rollcall}{data.frame with rollcall voting data (legislators as rows, votes as columns)
Values: 1 (Yes), -1 (No), 0 (Abstention), NA (Absent)}

\item{distance_method}{Integer, distance metric for clustering (1 = Manhattan, 2 = Euclidean)}

\item{pivot}{Character, optional pivot legislator name (NULL for automatic selection from 'right' cluster)}

\item{threshold}{Numeric, minimum participation threshold (0.0-1.0)}

\item{verbose}{Logical, whether to print progress messages}
}
\value{
List with:
  - results: data.frame with d1, d2, auto_cluster for each legislator
  - bcall_object: BCall R6 object
  - clustering_object: Clustering R6 object
  - metadata: Analysis metadata
}
\description{
PRINCIPLE 1: User provides only rollcall data.
This function automatically clusters legislators into two groups and runs B-Call analysis.
}
\examples{
# Create sample rollcall data
rollcall <- data.frame(
  vote1 = c(1, -1, 1, -1, 1, -1),
  vote2 = c(1, -1, 1, -1, 1, -1),
  vote3 = c(-1, 1, -1, 1, -1, 1),
  vote4 = c(1, 1, -1, -1, 1, -1),
  row.names = paste0("Legislator_", LETTERS[1:6])
)

# Run automatic clustering analysis
results <- bcall_auto(rollcall, distance_method = 1, threshold = 0.1, verbose = FALSE)

# View results
head(results$results)

# Visualize
\donttest{
plot_bcall_analysis(results)
plot_bcall_analysis_interactive(results)
}

}
