% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcall_main.R
\name{bcall}
\alias{bcall}
\title{B-Call Analysis with User-Provided Clustering}
\usage{
bcall(rollcall, clustering, pivot, threshold = 0.1, verbose = TRUE)
}
\arguments{
\item{rollcall}{data.frame with rollcall voting data (legislators as rows, votes as columns)
Values: 1 (Yes), -1 (No), 0 (Abstention), NA (Absent)}

\item{clustering}{data.frame with one column OR named vector with exactly 2 unique values}

\item{pivot}{Character, pivot legislator name}

\item{threshold}{Numeric, minimum participation threshold (0.0-1.0)}

\item{verbose}{Logical, whether to print progress messages}
}
\value{
List with:
  - results: data.frame with d1, d2, cluster for each legislator
  - bcall_object: BCall R6 object
  - metadata: Analysis metadata
}
\description{
PRINCIPLE 2: User provides rollcall data AND clustering assignment.
This function runs B-Call analysis using the user's clustering.
}
\examples{
# Create sample rollcall data
rollcall <- data.frame(
  vote1 = c(1, -1, 1, -1, 1, -1),
  vote2 = c(1, -1, 1, -1, 1, -1),
  vote3 = c(-1, 1, -1, 1, -1, 1),
  row.names = c("Leg_A", "Leg_B", "Leg_C", "Leg_D", "Leg_E", "Leg_F")
)

# Create user-defined clustering (can use any 2 values)
clustering <- data.frame(
  cluster = c("left", "right", "left", "right", "left", "right"),
  row.names = c("Leg_A", "Leg_B", "Leg_C", "Leg_D", "Leg_E", "Leg_F")
)

# Run analysis with user clustering
results <- bcall(rollcall, clustering, pivot = "Leg_B", threshold = 0.1, verbose = FALSE)

# View results
head(results$results)

# Visualize
\donttest{
plot_bcall_analysis(results)
plot_bcall_analysis_interactive(results)
}

}
